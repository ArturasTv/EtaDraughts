const ApiRoutes = {
  ROOT: {
    INDEX: 'api',
  },
  AUTH: {
    CALLBACK: {
      INDEX: '/api/auth/callback',
    },
    UPDATE_PASSWORD: {
      INDEX: '/api/auth/update-password',
    },
  },
};

export default ApiRoutes;
